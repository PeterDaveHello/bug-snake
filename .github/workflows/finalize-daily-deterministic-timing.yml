name: Retired daily challenge finalizer

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: echo 'This isolated finalizer has completed and performs no repository writes.'
